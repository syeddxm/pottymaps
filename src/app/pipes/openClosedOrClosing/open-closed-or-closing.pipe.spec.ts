import { OpenClosedOrClosingPipe } from './open-closed-or-closing.pipe';

describe('OpenClosedOrClosingPipe', () => {
  it('create an instance', () => {
    const pipe = new OpenClosedOrClosingPipe();
    expect(pipe).toBeTruthy();
  });
});
