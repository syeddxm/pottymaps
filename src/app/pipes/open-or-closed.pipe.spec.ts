import { OpenOrClosedPipe } from './open-or-closed.pipe';

describe('OpenOrClosedPipe', () => {
  it('create an instance', () => {
    const pipe = new OpenOrClosedPipe();
    expect(pipe).toBeTruthy();
  });
});
